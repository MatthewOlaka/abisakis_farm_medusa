{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build; EXIT_CODE=$?; echo '=== BUILD EXIT CODE:' $EXIT_CODE '==='; ls -la .medusa/ 2>&1; ls -la .medusa/admin/ 2>&1; ls -la .medusa/server/ 2>&1; exit $EXIT_CODE"
  },
  "deploy": {
    "startCommand": "npx medusa db:migrate && npx medusa start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
