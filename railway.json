{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && echo '=== BUILD OUTPUT ===' && ls -la .medusa/ && ls -la .medusa/admin/ 2>/dev/null && ls -la .medusa/server/ 2>/dev/null || echo 'admin dir missing'"
  },
  "deploy": {
    "startCommand": "npx medusa db:migrate && npx medusa start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
